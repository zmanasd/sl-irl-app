import SwiftUI

/// Settings / Setup screen.
/// Currently hosts the ConnectionsView for Phase 3.
struct SettingsView: View {
    var body: some View {
        ConnectionsView()
    }
}

#Preview {
    SettingsView()
}
